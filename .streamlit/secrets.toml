# 複製此檔為 secrets.toml，填入你的 Supabase 連線字串
# secrets.toml 已加入 .gitignore，不會被 commit

DATABASE_URL = "postgresql://postgres:b6@6py,Ad7n*j*A@db.xxxx.supabase.co:5432/postgres"

cd Taipower
pip install -r requirements.txt

# 建立本機 secrets 檔（不會被 git commit）
cp .streamlit/secrets.toml.example .streamlit/secrets.toml
# 用文字編輯器把 secrets.toml 裡的 URL 換成你的新密碼版本

streamlit run app.py
